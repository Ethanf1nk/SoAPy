name: SoAPy
channels:
  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - setuptools
  
    # Package Depends

  - scipy
  - h5py
  - requests
  - matplotlib

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

