name: SoAPy
channels:
  - condaforge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
    # Package Depends

  - scipy
  - h5py
  - shutil
  - requests
  - matplotlib

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

